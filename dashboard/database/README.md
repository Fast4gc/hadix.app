# dashboard/database

Reservado para migrações/esquema (ex: SQLite ou Postgres) caso o dashboard
web/api precise de persistência além dos arquivos JSON em `config/`.

Ainda não implementado nesta versão.
